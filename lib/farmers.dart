import 'package:flutter/material.dart';

class Farmers extends StatelessWidget {
  const Farmers({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Farmers Page"),
      ),
    );
  }
}
